# ============================================================
# MediScan AI — Production Dependencies
# (torch + torchvision are installed separately in Dockerfile
#  using CPU-only wheels to keep image size small)
# ============================================================

# --- Web Framework -----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.1

# --- Settings & Validation ---------------------------------------------------
pydantic==2.7.1
pydantic-settings==2.3.4
pydantic[email]

# --- Authentication ----------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- File Handling -----------------------------------------------------------
python-multipart==0.0.9
PyPDF2==3.0.1
Pillow==10.3.0

# --- NLP / Transformers ------------------------------------------------------
transformers==4.41.0
sentencepiece==0.2.0

# --- Utilities ---------------------------------------------------------------
python-dotenv==1.0.1
